# 小虫的影视生产流｜工作区约定

- 用户于 2026-09-04 将本轮新建并正式冻结的系统命名为“小虫的影视生产流”。后续系统规范、部门 Skill 制作及交付统一使用这一名称。
- 本系统与旧仓库系统、SKillXC 的 30 个旧版生产与控制 Skill 分开管理。不得把旧 Skill 数量、实现状态、调用路径或规则当作本系统的现状。
- 本系统以用户已确认的冻结稿及后续明确修订为准。入口见 `影视生产系统规范/README.md`；共享资源入口见 `共享能力库/目录.md`。规范冻结不等于 Skill 已制作、安装或联调完成。
- SKillXC 的 30 个旧 Skill 暂时封存。封存期间不自动加载、调用、继续改造、重新安装或恢复旧版生产路由；旧源码及原有资料保留。只有用户明确要求查看、比较或恢复旧版时，才在指定范围内处理。
- `SKillXC/`、旧 `交付包/` 与 `film-production-codex-skills/` 不作为新系统的默认规则入口。已明确收存到共享能力库的 Profile、专业 Skill 及待适配源文件，按库内版本与状态使用，不因旧仓库封存而一并停用。
- 本次命名与旧版封存不改变已冻结的岗位职责、执行流程和输出约定。修改冻结业务条款仍按用户授权范围处理。
- 跨任务信息以用户提交或明确指定并实际读取的资料为准，不声称自动掌握其他任务的工作状态。
- 本仓库提供 22 个 `xcflow-` 岗位的完整安装源。当前电脑是否安装以实际检查为准，不能沿用历史本机回执判断。使用入口见根目录 `启动工作流.md`，实际岗位映射见 `小虫的影视生产流-Skills/skills/xcflow-control/references/roles.md`。按当前请求读取最小必要岗位，不启动全部部门；已授权任务在当前任务持续承接。规范正文保持冻结，部署和联调结果另记于接入报告。

## 工作电脑接入

- 用户要求安装、重绑或按启动指南启动时，读取根目录 `启动工作流.md`，运行 `安装工作流.py check`、`install` 和 `check-installed`，按本机实际位置接入。首次启动指令已授权这些安装步骤，不重复索取安装许可。
- 检查及安装只读取本仓库和本机安装位置。仓库中旧本机联调记录、冻结页头的部署描述、原始来源路径是历史证据，不代表本机已加载、已创建任务或已经制作成果。
- 运行岗位使用 `小虫的影视生产流-Skills/skills` 下的入口及其规范。`跨电脑接入/原本机实现` 只保存旧安装实现用于追溯，不作为当前安装程序执行。
- 根目录安装程序只接入新 22 岗位，保留其他安装与宿主配置。共享能力依旧由执行单元询问、用户确认需求、专业控制部查询、用户准确选版、实际读取后回接原执行单元。
- 总控启动后按用户给定的具体项目目标工作；没有项目资料时只说明已进入总控并等待需求，不编造项目、不自动创建新任务、不批量启动所有部门。
