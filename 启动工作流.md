# 小虫的影视生产流｜工作电脑启动指南

## 一条首次启动指令

把下面这段发给工作电脑上的 Codex：

> 请从 https://github.com/sla20110526-alt/XiaoC-YingShiGongZuo 获取“小虫的影视生产流”完整仓库；本地已有时直接使用，未下载时克隆到当前工作区的独立文件夹，不覆盖已有项目。读取仓库根目录的 AGENTS.md 和《启动工作流.md》，检查完整文件，按指南完成这台电脑的安装或路径重绑，核对 22 个新岗位 Skill、冻结规范与共享能力库可读取。随后在当前任务启动“小虫的影视生产流｜总控”，等待我提交具体项目需求。沿用冻结规范与共享库准确版本，保持旧 SKillXC 的 30 个旧版 Skill 封存；不自动创建新任务，不批量启动全部部门。

## Codex 执行步骤

1. 定位本地仓库根目录，实际读取 AGENTS.md 和本指南。没有本地副本时按上面请求克隆；已有副本不得用下载或更新覆盖未提交的本地改动。
2. 在根目录使用 Python 3.10 或更新版本执行下面三项操作；Windows 可用 `python` 或 `py -3`。只缺运行环境时说明实际缺口，不把未执行报成已安装。

```text
python 安装工作流.py check
python 安装工作流.py install
python 安装工作流.py check-installed
```

3. 检查安装程序返回的本机安装目录与回执。回执是 `.xcflow-install-receipt.json`，位于实际技能安装目录内；不要依据仓库历史报告中的家里电脑路径判断本机状态。
4. 实际读取本机安装目录下 `xcflow-control/SKILL.md`、它指向的运行说明，以及总控的岗位职责、执行流程和输出约定。按本次启动范围承接，不需要读取所有岗位或加载全库专业能力。
5. 需要验证宿主自动发现时，执行下方可用性检查，或依据当前 Codex 实际技能列表确认。检查程序只调用技能发现接口，不创建模型任务。

```text
python 小虫的影视生产流-Skills/制作与检查/discover.py
```

6. 说明本机已完成的安装与读取状态，在当前任务以总控身份等待用户的项目需求。如果界面列表未刷新，可以重新打开任务；有准确路径且已读取时，也可先按该岗位文件承接，不声称尚未观察到的自动发现已完成。

当前环境没有终端工具时，仍可说明手动安装方式并提供完整启动文本；未执行的安装与读取保持待完成状态。

## 安装以后日常启动

> 在当前任务启动“小虫的影视生产流｜总控”。本次项目是……，需要完成……，相关资料在……。请按冻结规范处理，只启动本次需要的部门。

可以直接指定任一岗位，无须每次经过总控或跑完整链路。只说“启动岗位”默认留在当前任务；明确说“创建新任务”才创建。

执行单元询问专业能力时，确认需要后会得到具体候选。你选定准确版本与范围后，能力才被实际读取并用于原执行单元工作。整套系统不自动知道其他任务的进度；需要你回传或明确指定读取范围。
